name: Keep Render App Alive

on:
  schedule:
    - cron: "*/10 * * * *"  # every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render app
        run: |
          curl -fsS https://blogsite-rhfp.onrender.com/ > /dev/null || echo "Ping failed"
